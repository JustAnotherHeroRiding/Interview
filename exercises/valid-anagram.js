/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function (s, t) {
  let count = Set()
  
  console.log(sCount);
  console.log(tCount);

  console.log(sCount == tCount);
};

s = "anagram";
t = "nagaram";

isAnagram(s, t);
